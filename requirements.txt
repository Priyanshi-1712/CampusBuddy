# --- Core Framework ---
fastapi==0.109.0
uvicorn[standard]==0.27.0  # Server to run FastAPI

# --- Database & Models ---
sqlalchemy==2.0.25         # ORM for PostgreSQL
psycopg2-binary==2.9.9     # PostgreSQL driver
pydantic[email]==2.5.3     # Data validation & Email checks
alembic==1.13.1            # Database migrations

# --- Security & Authentication ---
python-jose[cryptography]==3.3.0  # JWT Token generation
passlib[bcrypt]==1.7.4            # Password hashing
python-multipart==0.0.6           # Required for OAuth2 forms

# --- Real-time & Communication ---
websockets==12.0           # For Live Ride Tracking
httpx==0.26.0              # To call external APIs (like SMS/Email gateways)

# --- Payments & Integration ---
razorpay==1.4.1            # Official Razorpay SDK
python-dotenv==1.0.1       # To manage your .env secrets

# --- Utilities ---
pydantic-settings==2.1.0   # Settings management
emails==0.6                # Sending OTPs via SMTP